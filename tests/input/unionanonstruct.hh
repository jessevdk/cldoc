/*
 * The union A.
 *
 * A longer description of A.
 */
union A
{
	struct {
		/* The a field. */
		float a;

		/* The b field. */
		float b;
	};

	struct {
		/* The x field. */
		float x;

		/* The y field. */
		float y;
	};
};
